//  import SignInPage from "@/app/auth/signin/page";
// import { useCallback } from "react";

// export default function LoginModalForm({ onClose }: { onClose?: () => void }) {
//   // Pass onClose to SignInPage if needed
//   return <SignInPage onClose={onClose} />;
// }
